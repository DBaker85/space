import { Meta, ColorPalette, ColorItem } from "@storybook/addon-docs/blocks";

import { theme } from "../src/theme";

<Meta title="UI/Colors" />

export const getColors = ({ colors }) =>
  Object.keys(colors).map((key) => ({ name: key, value: colors[key] }));

# Colors

<div id="dark" title="Dark mode">
  <ColorPalette>
    {getColors(theme).map(({ name, value }) => (
      <ColorItem
        key={name}
        title={`theme.colors.${name}`}
        subtitle={name}
        colors={[value]}
      />
    ))}
  </ColorPalette>
</div>
